language: python

python:
  - "2.7"
  - "3.4"
install:
  - pip install --upgrade pip
  - pip install scipy
  - pip install -e .
  - pip install pytest
script:
  - py.test tests/Test_data.py
notifications:
  email: false