%if extra != None: 
${extra}
%endif
%for i in list:
% if not isinstance(i, str):
    %if addition != None:
${addition}${i.name.replace(" ", "")}
    %else:
${i.name.replace(" ", "")}
    %endif
% else:
    %if addition != None:
${addition}${i.replace(" ", "")}
    %else:
${i.replace(" ", "")}
    %endif
%endif
%endfor